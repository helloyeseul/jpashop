spring:
### 사실 없어도 됨. (스프링부트가 자동으로 설정해 줌)
#  datasource:
### 메모리 DB 사용
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
### 실행할때 create 끝나면 drop table
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        #      show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace